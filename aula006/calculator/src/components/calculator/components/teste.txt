digitar no input; aceita so nums; digita com teclado;

ao apertar no = transforma oq ta no input em num, e manda pro historico um obj com calc e result;

limpar o historico, limpa o array;

sempre formatar os num, exemplo: 900.310,7

caso resultado >= 46 DIGITOS ? return "numero muito grande"

caso resultado > 16 DIGITOS ? return transforma em potencia (

    numeroDeDigitos > 16 ? {
        mantissa = numero.toString().slice(0, 6)
        mantissaDecimal = mantissa[0] + "." + mantissa.slice(1)
        expoente = numeroDeDigitos - 1

        numero = mantissaDecimal + "e+" + expoente
    }
)

ao usar o teclado os efeitos de pressionado acontecem

caso resultado = infinity return "infinito"; caso retorne resultado diferentes de numero como NaN retorn "numero invalido"



